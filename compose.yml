services:
  development:
    build:
      context: .
      dockerfile: Containerfile
    container_name: container-dev

    image: container-dev:latest
    ports:
      - "3000:80"
    environment:
      VITE_API_URL: https://api.miproduccion.com
      VITE_APP_NAME: Nombre de mi aplicacion
    # solo en desarrollo
    # volumes:
    #   - ./dist:/usr/share/nginx/html
