services:
  development:
    build:
      context: .
      dockerfile: Containerfile
    container_name: container-dev

    image: container-dev:latest
    ports:
      - "3000:80"
    environment:
      API_URL: http://192.168.0.17:3001/
    # solo en desarrollo
    # volumes:
    #   - ./dist:/usr/share/nginx/html
